import 'package:bimmer_wheels/models/tile_model.dart';

// This widget will slide up from the bottom of the screen displaying information about the wheel.
// like size, width, offset, model(s) it came off of. 

class DisplayWheelData {
  final Tile card;

  DisplayWheelData({required this.card});
}